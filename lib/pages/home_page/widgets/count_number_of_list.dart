import 'package:flutter/material.dart';

class CountNumberOfList extends StatelessWidget {
  final int total;
  final int completed;

  const CountNumberOfList({
    Key? key,
    this.total = 0,
    this.completed = 0,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
